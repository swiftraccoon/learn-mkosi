# /var Partition
# CIS Benchmark 1.1.2: Ensure separate partition for /var
# Contains variable data: logs, spools, caches
# Mount options: nodev, nosuid (prevent devices and setuid)

[Partition]
Type=var
Format=btrfs
SizeMinBytes=10G
SizeMaxBytes=50G
MountPoint=/var:nodev,nosuid,compress=zstd
